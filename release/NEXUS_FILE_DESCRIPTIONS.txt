NEXUS — TEXTE FUER DIE DATEI-UPLOADS
====================================
Nexus erlaubt im Feld "File description" nur 255 Zeichen. Der laengere
Text gehoert ins Feld "Changelog" darunter. Beides ohne externe Links.


==================================================================
DATEI 1 — Main Files
Name:          S2Tweaker 1.19.1
File version:  1.19.1
File category: Main Files
==================================================================

--- File description (244 Zeichen) -------------------------------
The tool itself. Extract everything into one folder, keep _internal next to the exe, run S2Tweaker.exe. One extra file is needed to read your game - the tool shows you where to get it. VirusTotal 1/66, only Zillya, reported as a false positive.
------------------------------------------------------------------

--- Changelog ----------------------------------------------------
1.19.1 - nothing is downloaded while the tool runs

- The tool no longer fetches anything at runtime. It used to download the
  Oodle decompression library on first use and load it, which is exactly
  what a dropper does and one reason scanners flagged it. That code is
  gone. A setup window now walks you through placing that file once, with
  pictures, and shows where it goes. It only appears when the file is
  missing, and an indicator in the header shows the state at a glance.
- The pak tool it bundles (repak) is built from source with its download
  function and whole HTTP/TLS stack removed, so it cannot fetch anything
  either. It is no longer a third-party prebuilt binary.
- update.bat is no longer inside the download. It is available here as an
  optional file instead - a script that downloads a release and replaces
  program files should be your choice, not a surprise.
- Sorry about the one manual step. The library may not be shipped, cannot
  be taken out of the game (it is compiled into the game executable), and
  the open re-implementations have no licence. Doing it automatically was
  the old answer, and that is what got this tool flagged.
- No new game files, no re-extraction. Your existing paks stay valid.

1.19.0 - shipped as a folder instead of a single exe
- The exe was a self-extracting archive that unpacked into your temp
  folder and ran from there - harmless, but the shape of a dropper. Now a
  3 MB launcher next to an "_internal" folder. Keep them together.
- The exe finally identifies itself: publisher, product, version, licence.
- Fixed three weapons offering a "Durability" slider that could never do
  anything (they have no durability value in the game data).
- New test suite checking every slider against your game's real values:
  over 3,000 checks, no wrong values found.
------------------------------------------------------------------


==================================================================
DATEI 2 — Optional files
Name:          update.bat (optional one-click updater)
File version:  1.19.1
File category: Optional files
==================================================================

--- File description (232 Zeichen) -------------------------------
Optional. Put it next to S2Tweaker.exe and the tool can update itself in one click, old files kept as .bak. You do not need it - without it you update by hand. Deliberately not inside the main download. Plain text. VirusTotal: 0/60.
------------------------------------------------------------------

--- Changelog ----------------------------------------------------
Split out of the main download in 1.19.1.

A script that fetches a release and replaces program files is one of the
patterns antivirus scanners dislike, so it no longer travels inside the
package unasked - you decide whether you want it.

To use it: save it next to S2Tweaker.exe. "Check for updates" in the tool
then offers a one-click swap, keeping your previous files as .bak. Your
settings, presets, cache and built mods are never touched.

Without it nothing breaks: "Check for updates" still tells you when a new
version exists and opens the download page, you just replace the files
yourself.

It is plain text on purpose - open it in Notepad and read exactly what it
does before you run it.
------------------------------------------------------------------
